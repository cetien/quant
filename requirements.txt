# ============================================================
# requirements.txt — Quant Platform
# ============================================================

# ── 핵심 데이터 처리 ──────────────────────────────────────
duckdb>=0.10.0
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0          # DuckDB-Pandas Zero-Copy 필수

# ── 데이터 수집 ────────────────────────────────────────────
yfinance>=0.2.40          # 글로벌 지수·매크로 (SOX, S&P500, USD/KRW 등)
pykrx>=1.0.45             # KRX 일봉·수급·종목목록 (거래대금 포함)
finance-datareader>=0.9.50 # 보조 수집원 (import 이름은 FinanceDataReader)
tenacity>=8.2.0           # API Rate Limit 재시도 (exponential backoff)

# ── UI ────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0
streamlit-aggrid

# ── 유틸 ─────────────────────────────────────────────────
openpyxl>=3.1.0           # Excel 내보내기
python-dotenv>=1.0.0      # 환경변수 관리 (API 키 등)
